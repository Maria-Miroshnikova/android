<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hfad.healthstory">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthStory">
        <activity android:name=".ShowStoryImageActivity"></activity>
        <activity android:name=".FilterActivity"></activity>
        <activity android:name=".StoryActivity">
        </activity>
        <activity android:name=".CreateUserStoryActivity" />
        <activity android:name=".CreateUserDataActivity" />
        <activity android:name=".CreateUserPasswordActivity" />
        <activity android:name=".demo.AuthActivity_demo" />
        <activity android:name=".OptionsPageActivity"></activity>
        <activity android:name=".DataPageActivity" />
        <activity android:name=".PersMainPageActivity" />
        <activity android:name=".AuthActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>